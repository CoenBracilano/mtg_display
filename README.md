# mtg_display

## Usage
To run the online server, type in the following command while in the root directory:
```
python backend/server.py
```

[comment] The requirements.txt file is just ripped straight out of my pip freeze output so yknow whatevers